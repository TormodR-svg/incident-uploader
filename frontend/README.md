# Frontend (GitHub Pages)

Страницы:
- `index.html` — пользователи (с небольшой полосой прогресса)
- `admin.html` — администратор (проверка на стороне Worker)

Что заменить:
- в `index.html` и `admin.html`: `__GOOGLE_CLIENT_ID__`
- в `app.js` и `admin.js`: `__WORKER_URL__`
